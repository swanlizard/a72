SC2P:		PUSH AX
		PUSH CX
		PUSH SI
		PUSH DI
		MOV DI, SI
		MOV CX, 100H
		XOR AL, AL
		CLD
	REPNZ	SCASB
		JNZ SC2P0
		MOV AX, 0FFH
		SUB AX, CX
		DEC DI
		MOV SI, DI
		DEC DI
		MOV CX, AX
		STD
	REPZ	MOVSB
		STOSB
SC2P0:		POP DI
		POP SI
		POP CX
		POP AX
		RET
