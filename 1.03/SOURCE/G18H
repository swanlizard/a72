G18H:		CALL CC
		CMP AL, 3AH
		JNZ G18H1
		MOV AL, 40H
		MOV [FLAGS], AL
		MOV AL, 3EH
		MOV [SEGPREF], AL
		INC SI
		RET
G18H1:		CALL RE
		ADD [USIZE], AX
		ADD [PC], AX
		XOR AL, AL
		MOV [FLAGS], AL
		RET
