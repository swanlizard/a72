CALF:		MOV DI, OFFSET INCLEV+2
		MOV BX, OFFSET FNAMES
		MOV AX, [INCLEV]
		OR AX, AX
		JNZ CALF0
		RET
CALF0:		SHL AX, 1
		SHL AX, 1
		SHL AX, 1
		ADD DI, AX
		MOV BX, [DI-4]
		MOV AL, [BX]
		XOR AH, AH
		INC AX
		ADD BX, AX
		RET
