INVFD:		MOV AL, [OPCODE]
		MOV AH, [MODRM]
		MOV CL, 3
		ROR AH, CL
		AND AX, 707H
		ROL AL, CL
		OR AL, AH
		XOR AH, AH
		CALL WHA
		MOV AL, 2CH
		STOSB
DISPDD:		MOV AL, [MODRM]
		CALL WDISP
		JMP ENDL
