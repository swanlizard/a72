INIT:		XOR AX, AX
		MOV [PC], AX
		MOV [ERRS], AX
		MOV [USIZE], AX
		MOV [UD], AX
		MOV [INCLEV], AX
		MOV [FNAMES], AX
		CMP BYTE PTR [FUNC], 2
		JZ NOCSYM
		MOV [SYMBS], AX
NOCSYM:		INC AH
		MOV [VORG], AX
		RET
