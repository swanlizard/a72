G16H:		CALL CC
		CMP AL, 3AH
		JNZ G16H1
		MOV AL, [FLAGS]
		AND AL, 0BFH
		OR AL, 40H
		MOV [FLAGS], AL
		MOV AL, 3EH
		MOV [SEGPREF], AL
		INC SI
		JMP ASML
G16H1:		CALL RE
		ADD [USIZE], AX
		ADD [PC], AX
		XOR AL, AL
		MOV [FLAGS], AL
		RET
