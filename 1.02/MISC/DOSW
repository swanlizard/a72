DOSW:		PUSH AX
		PUSH CX
		PUSH DX
		PUSH DI
		MOV DI, SI
		XOR AL, AL
		MOV CX, 100H
		CLD
	REPNZ	SCASB
		JNZ DOSW0
		DEC DI
		PUSH DI
		MOV AL, 24H
		STOSB
		MOV AH, 9
		MOV DX, SI
		INT 21H
		POP DI
		XOR AL, AL
		STOSB
DOSW0:		POP DI
		POP DX
		POP CX
		POP AX
		RET
