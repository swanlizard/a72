INCF:		CLD
		PUSH DI
		MOV AX, [INFILE]
		STOSW
		XOR AX, AX
		STOSW
		STOSW
		STOSW
		MOV CX, 40H
		MOV DI, SI
	REPNZ	SCASB
		NEG CX
		ADD CX, 3FH
		POP DI
		MOV [DI+4], BX
		MOV DI, BX
		MOV [DI], CL
		INC DI
	REPZ	MOVSB
		RET
