G18H:		LODSW
		CMP AX, 3A01H
		JNZ G18H1
		MOV AL, 40H
		MOV [FLAGS], AL
		MOV AL, 3EH
		MOV [SEGPREF], AL
		RET
G18H1:		DEC SI
		DEC SI
		CALL RE
		XOR AL, AL
		MOV [FLAGS], AL
		MOV AX, [IMM]
		ADD [USIZE], AX
		ADD [PC], AX
		RET
