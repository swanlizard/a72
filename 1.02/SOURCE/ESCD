ESCD:		MOV AL, [OPCODE]
		MOV AH, [SI]
		ROR AH, 1
		ROR AH, 1
		ROR AH, 1
		AND AX, 707H
		ROL AL, 1
		ROL AL, 1
		ROL AL, 1
		OR AL, AH
		XOR AH, AH
		CALL WHA
		MOV AL, 2CH
		STOSB
		LODSB
		CALL WDISP
		JMP ENDL
