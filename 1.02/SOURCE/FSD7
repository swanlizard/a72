FSD7:		MOV AL, [MODRM]
		PUSH AX
		AND AL, 8
		SHR AL, 1
		SHR AL, 1
		XOR AH, AH
		MOV DX, AX
		ADD DX, OFFSET _SG7
		XOR AL, AL
		XCHG AL, [FPREF]
		CMP AL, 9BH
		POP AX
		JNZ FSD70
		ADD DX, 4
FSD70:		CALL GRPF
		CALL WDISP
		JMP ENDL
